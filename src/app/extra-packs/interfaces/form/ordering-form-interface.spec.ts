import { OrderingFormInterface } from './ordering-form-interface';

describe('OrderingFormInterface', () => {
  it('should create an instance', () => {
    expect(new OrderingFormInterface()).toBeTruthy();
  });
});
